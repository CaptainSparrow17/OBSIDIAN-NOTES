
##### Nameserver
A nameserver is the Domain Name System (DNS) server component that translates a website’s domain name into a numerical IP address. This term can refer to any server with DNS software, as well as a hosting provider’s server that manages customer domain names.

##### Authoritative DNS Server (a tho re tey tive)
The Authoritative DNS server is the final holder of the IP of the domain you are looking for. It is the name server which has the original zone records.

##### Nameserver vs Authoritative Name Server
A "name server" is a broad term for any server that answers Domain Name System (DNS) queries. An "authoritative server" is a specific type of name server that holds the definitive, original DNS records for a domain.
##### DNS
The Domain Name System (DNS) protocol keeps records of which domain names correspond to specific IP addresses. DNS enables you to browse the web by typing in regular URLs instead of IP addresses.

##### Does DNS store anything?
❌ DNS itself = NO storage
DNS is just the system/rules.
But DNS uses servers that DO store data.

##### Where is the data actually stored?
The data (called **DNS records**) is stored on:
👉 **Name servers**
These are real servers that hold information like:
- `google.com → IP address`
- email servers (MX records)
- subdomains (`mail.google.com`)
So:
> Name servers = storage of DNS records

##### What exactly is a name server?
A **name server** is just a computer/server that:
- stores DNS records for a domain
- responds when asked “what is the IP for this domain?”
Think:
👉 It is like a database server for domain names.

##### Simple analogy (very important)
Imagine a phone system:

|Internet|Real world|
|---|---|
|DNS|Phone system rules|
|Name server|Phone directory office|
|DNS record|Phone number entry|

So:
- DNS = system that knows how to find numbers
- Name server = place that stores numbers
- DNS record = the number itself

---

| **Server Type**                | **Description**                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |     |
| ------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- |
| `DNS Root Server`              | The root servers of the DNS are responsible for the top-level domains (`TLD`). As the last instance, they are only requested if the name server does not respond. Thus, a root server is a central interface between users and content on the Internet, as it links domain and IP address. The [Internet Corporation for Assigned Names and Numbers](https://www.icann.org/) (`ICANN`) coordinates the work of the root name servers. There are `13` such root servers around the globe. |     |
| `Authoritative Nameserver`     | Authoritative name servers hold authority for a particular zone. They only answer queries from their area of responsibility, and their information is binding. If an authoritative name server cannot answer a client's query, the root name server takes over at that point. Based on the country, company, etc., authoritative nameservers provide answers to recursive DNS nameservers, assisting in finding the specific web server(s).                                              |     |
| `Non-authoritative Nameserver` | Non-authoritative name servers are not responsible for a particular DNS zone. Instead, they collect information on specific DNS zones themselves, which is done using recursive or iterative DNS querying.                                                                                                                                                                                                                                                                               |     |
| `Caching DNS Server`           | Caching DNS servers cache information from other name servers for a specified period. The authoritative name server determines the duration of this storage.                                                                                                                                                                                                                                                                                                                             |     |
| `Forwarding Server`            | Forwarding servers perform only one function: they forward DNS queries to another DNS server.                                                                                                                                                                                                                                                                                                                                                                                            |     |
| `Resolver`                     | Resolvers are not authoritative DNS servers but perform name resolution locally in the computer or router.                                                                                                                                                                                                                                                                                                                                                                               | `   |

Domain Name System` (`DNS`) is an integral part of the Internet. For example, through domain names, such as [academy.hackthebox.com](https://academy.hackthebox.com/) or [www.hackthebox.com](https://www.hackthebox.com/), we can reach the web servers that the hosting provider has assigned one or more specific IP addresses. DNS is a system for resolving computer names into IP addresses, and it does not have a central database. Simplified, we can imagine it like a library with many different phone books. The information is distributed over many thousands of name servers. Globally distributed DNS servers translate domain names into IP addresses and thus control which server a user can reach via a particular domain. There are several types of DNS servers that are used worldwide:

- DNS root server
- Authoritative name server
- Non-authoritative name server
- Caching server
- Forwarding server
- Resolver

1)Browser -> www.google.com
2)Your computer checks the hosts file first. It acts as a static, local override. If the domain isn’t found there, your system then checks the DNS resolver cache and finally queries external DNS servers.
computer checks, browser and OS cache, check if request made earlier. Thus no need of hastle and gets ip address instantly.
Cache is remained for some time, if cache is missed
3)Then DNS Resolver is contacted, mainly hosted by ISP, or 3rd party DNS resolver Cloudflare or google.
4)check ISP resolver cache, checks if request made in past. Cache is for speeding up dns resolving process fast. Again cache is missed
5)Then DNS resolver contact ROOT Nameserver.  Root Server is the top of hirarchey. There are total 13 root server.
	DNS Resolver reaches to a random nameserver suppose 5th one. %th will say its IP OR If missed, 5th will say look at TLD name servers. EG: .com TLD (Top Level Domain) name server
6)TLD nameserver (.com, .edu, .net), stores the address information for the top level domain.
The TLD then direct the DNS resolver  to  authoritative name server
7)Authoritative name server gives the IP address. Then DNS Resolver provides IP to browser
8)Thus browser request the IP address.
(DNS Resolver stores the IP in cache memory for some time)

Authorative Name Server are responsible for knowing everything about Domain which includes IP addresses.
![](../../Attachements/Pasted%20image%2020260627205935.png)

DNS is mainly unencrypted. Devices on the local WLAN and Internet providers can therefore hack in and spy on DNS queries. Since this poses a privacy risk, there are now some solutions for DNS encryption. By default, IT security professionals apply `DNS over TLS` (`DoT`) or `DNS over HTTPS` (`DoH`) here. In addition, the network protocol `DNSCrypt` also encrypts the traffic between the computer and the name server.

However, the DNS does not only link computer names and IP addresses. It also stores and outputs additional information about the services associated with a domain. A DNS query can therefore also be used, for example, to determine which computer serves as the e-mail server for the domain in question or what the domain's name servers are called.
![](../../Attachements/Pasted%20image%2020260627210105.png)

Different `DNS records` are used for the DNS queries, which all have various tasks. Moreover, separate entries exist for different functions since we can set up mail servers and other servers for a domain.

|**DNS Record**|**Description**|
|---|---|
|`A`|Returns an IPv4 address of the requested domain as a result.|
|`AAAA`|Returns an IPv6 address of the requested domain.|
|`MX`|Returns the responsible mail servers as a result.|
|`NS`|Returns the DNS servers (nameservers) of the domain.|
|`TXT`|This record can contain various information. The all-rounder can be used, e.g., to validate the Google Search Console or validate SSL certificates. In addition, SPF and DMARC entries are set to validate mail traffic and protect it from spam.|
|`CNAME`|This record serves as an alias for another domain name. If you want the domain [www.hackthebox.eu](http://www.hackthebox.eu/) to point to the same IP as hackthebox.eu, you would create an A record for hackthebox.eu and a CNAME record for [www.hackthebox.eu](http://www.hackthebox.eu/).|
|`PTR`|The PTR record works the other way around (reverse lookup). It converts IP addresses into valid domain names.|
|`SOA`|Provides information about the corresponding DNS zone and email address of the administrative contact.|
##### SOA RECORDS
```shell
dig soa www.inlanefreight.com
```
The `SOA` record is located in a domain's zone file and specifies who is responsible for the operation of the domain and how DNS information for the domain is managed.
The dot (.) is replaced by an at sign (@) in the email address. In this example, the email address of the administrator is `awsdns-hostmaster@amazon.com`.
![](../../Attachements/Pasted%20image%2020260627210921.png)


### Default Configuration
There are many different configuration types for DNS. Therefore, we will only discuss the most important ones to illustrate better the functional principle from an administrative point of view. All DNS servers work with three different types of configuration files:

1. local DNS configuration files
2. zone files
3. reverse name resolution files

The DNS server [Bind9](https://www.isc.org/bind/) is very often used on Linux-based distributions. Its local configuration file (`named.conf`) is roughly divided into two sections, firstly the options section for general settings and secondly the zone entries for the individual domains. The local configuration files are usually:

- `named.conf.local`
- `named.conf.options`
- `named.conf.log`

It contains the associated RFC where we can customize the server to our needs and our domain structure with the individual zones for different domains. The configuration file `named.conf` is divided into several options that control the behavior of the name server. A distinction is made between `global options` and `zone options`.

Global options are general and affect all zones. A zone option only affects the zone to which it is assigned. Options not listed in named.conf have default values. If an option is both global and zone-specific, then the zone option takes precedence.

##### Local DNS Configuration
```shell
cat /etc/bind/named.conf.local
cat /etc/bind/db.domain.com
cat /etc/bind/db.10.129.14
```
![](../../Attachements/Pasted%20image%2020260627212353.png)

(Go through other resources)
##### DIG-NS Query
The footprinting at DNS servers is done as a result of the requests we send. So, first of all, the DNS server can be queried as to which other name servers are known. We do this using the NS record and the specification of the DNS server we want to query using the `@` character. This is because if there are other DNS servers, we can also use them and query the records. However, other DNS servers may be configured differently and, in addition, may be permanent for other zones.

```shell
dig ns inlanefreight.htb @10.129.14.128
```
![](../../Attachements/Pasted%20image%2020260627212933.png)


##### DIG-Version Query
Sometimes it is also possible to query a DNS server's version using a class CHAOS query and type TXT. However, this entry must exist on the DNS server. For this, we could use the following command:
```shell
dig CH TXT version.bind 10.129.120.85
```
![](../../Attachements/Pasted%20image%2020260627213028.png)

##### DIG - ANY Query
We can use the option `ANY` to view all available records. This will cause the server to show us all available entries that it is willing to disclose. It is important to note that not all entries from the zones will be shown.

```shell
dig any inlanefreight.htb @10.129.14.128
```
![](../../Attachements/Pasted%20image%2020260627213112.png)




---
### HACKERSPLOIT YOUTUBE VIDEO
---
#### host
```shell
host hsploit.com
```

![](../../Attachements/Pasted%20image%2020260627215401.png)

```shell
host -t ns hsploit.com
host -t mx hsploit.com
```
![](../../Attachements/Pasted%20image%2020260627215703.png)
 Reverse dns lookup
	 NOTE:: WIll give error sometime due to cloudflare blocking the request doing reverse lookup
 ```shell
 host 1.1.1.1
 ```
 ![](../../Attachements/Pasted%20image%2020260627215841.png)

#### nslookup

```shell
nslookup hsploit.com
```
![](../../Attachements/Pasted%20image%2020260627220244.png)

other way to do::
![](../../Attachements/Pasted%20image%2020260627220334.png)

#### DIG => Swiss Army for DNS RECON
![](../../Attachements/Pasted%20image%2020260627220736.png)

```shell
dig hsploit.com

```
![](../../Attachements/Pasted%20image%2020260627221142.png)

```shell
dig hsploit.com -t mx
dig hsploit.com -t ns
dig hsploit.com -t AAAA
dig hsploit.com AAAA
```
![](../../Attachements/Pasted%20image%2020260627221311.png)

###### Canonical name
A CNAME (Canonical Name) record is a Domain Name System (DNS) setting that acts like an alias, forwarding one domain or subdomain to another hostname instead of a direct IP address.
It allows multiple domain names to resolve to the same IP address and simplifies the process of managing various services or subdomains by pointing them to a single destination domain. Notably, all CNAME records must direct to a domain name rather than an IP address. The ”www” subdomain is a common CNAME example.
```shell
dig hsploit.com -t CNAME
dig hsploit CNAME
```

###### Get only imp information::
```shell
dig hsploit.com -t ns +short
```
![](../../Attachements/Pasted%20image%2020260628075913.png)

###### One Liner
```shell
for ip in `dig hsploit.com +short`; do nmap $ip; done 
```
![](../../Attachements/Pasted%20image%2020260628080359.png)

#### DNS ZONE TRANSFER
A DNS zone transfer (AXFR/IXFR) is the process of replicating DNS records from a primary DNS server to a secondary server. It ensures consistent records across the authoritative infrastructure for redundancy and fault tolerance. Operating over TCP port 53, it can be a significant security vulnerability if left improperly configured.

###### Why Important?
Zone file contains IP address of hosts, server and so on. It gives idea of infrastructure.

**Name Server (NS):** A server in the DNS system that stores information about a domain and directs DNS queries to the authoritative DNS servers responsible for that domain.

**Simple definition for Anki:**

> **Name Server (NS): A DNS server that tells which DNS servers are authoritative for a domain.**



To answer this in a bit more context, a name server is a server that is "authoritative" (is authorized to respond to DNS requests) for your domain.

If you have a domain called example.com, you will have name servers associated with that domain in the root servers. [Here is an indepth discussion about how all of this works that I answered earlier.](http://www.reddit.com/r/webhosting/comments/121pbo/how_does_domain_know_where_your_web_host_is/)

For most people this will be your name servers for your hosting company ns1.somehostingcompany.com and ns2.somehostingcompany.com.

For "custom" name servers, what the hosting company is talking about is creating name servers under your domain. So instead of the ones for the hosting company, the domain will instead point to ns1.yourdomain.com and ns2.yourdomain.com.

In reality there is no difference between the two, and it's more of a cosmetic thing than anything else. However if you were running your own hosting company and reselling somebody else's services, then you might want to hide that from your customers.

#### Zone transfer using Host

```shell
host -t ns zonetransfer.me
host -l zonetransfer.me nsztm1.digi.ninja
```
### 1st command: host -t ns zonetransfer.me
Meaning:

- `host` = simple DNS lookup tool
- `-t ns` = ask specifically for **NS (Name Server) records**
- `zonetransfer.me` = domain you’re querying

What NS records are:

NS records tell you:

> “Which DNS servers are authoritative for this domain?”

So you’re basically asking:

> “Who is in charge of answering DNS questions for zonetransfer.me?”

Expected result (for this domain):

It returns something like:

- `nsztm1.digi.ninja`
- `nsztm2.digi.ninja`

These are the **authoritative name servers**.

### 2nd command: host -l zonetransfer.me nsztm1.digi.ninja
 Meaning:

- `-l` = **zone transfer request (AXFR via host tool)**
- `zonetransfer.me` = domain whose zone you want to dump
- `nsztm1.digi.ninja` = the DNS server you are asking

So you're saying:

> “Give me ALL DNS records for zonetransfer.me from this name server.”
#3) What is a zone transfer (AXFR)?

A **zone transfer** is:

> A full copy of all DNS records from an authoritative DNS server.

That includes:

- A records (IP addresses)
- MX records (mail servers)
- TXT records (often secrets 😬)
- SRV records
- Subdomains
- Internal hosts (if misconfigured)

![](../../Attachements/Pasted%20image%2020260628084330.png)

#### Zonetransfer using DIG

```shell
dig -t ns zonetransfer.me
dig axfr zonetransfer.me @nsztm1.digi.ninja.
```

![](../../Attachements/Pasted%20image%2020260628084633.png)

##### DNS RECON
Its an automated tools, better not use for practise
```shell
dnsrecon -d zonetransfer.me
dnsrecon -d zonetransfer.me -t axfr
```

---
Done with ALEXIS

---

## ZOne Transfer
`Zone transfer` refers to the transfer of zones to another server in DNS, which generally happens over TCP port 53. This procedure is abbreviated `Asynchronous Full Transfer Zone` (`AXFR`). Since a DNS failure usually has severe consequences for a company, the zone file is almost invariably kept identical on several name servers. When changes are made, it must be ensured that all servers have the same data. Synchronization between the servers involved is realized by zone transfer. Using a secret key `rndc-key`, which we have seen initially in the default configuration, the servers make sure that they communicate with their own master or slave. Zone transfer involves the mere transfer of files or records and the detection of discrepancies in the data sets of the servers involved.

The original data of a zone is located on a DNS server, which is called the `primary` name server for this zone. However, to increase the reliability, realize a simple load distribution, or protect the primary from attacks, one or more additional servers are installed in practice in almost all cases, which are called `secondary` name servers for this zone. For some `Top-Level Domains` (`TLDs`), making zone files for the `Second Level Domains` accessible on at least two servers is mandatory.

DNS entries are generally only created, modified, or deleted on the primary. This can be done by manually editing the relevant zone file or automatically by a dynamic update from a database. A DNS server that serves as a direct source for synchronizing a zone file is called a master. A DNS server that obtains zone data from a master is called a slave. A primary is always a master, while a secondary can be both a slave and a master.

The slave fetches the `SOA` record of the relevant zone from the master at certain intervals, the so-called refresh time, usually one hour, and compares the serial numbers. If the serial number of the SOA record of the master is greater than that of the slave, the data sets no longer match.

```shell
dig axfr inlanefreight.htb @10.129.78.129
```
![](../../Attachements/Pasted%20image%2020260704192621.png)

#### DIG - AXFR Zone Transfer - Internal
If the administrator used a subnet for the `allow-transfer` option for testing purposes or as a workaround solution or set it to `any`, everyone would query the entire zone file at the DNS server. In addition, other zones can be queried, which may even show internal IP addresses and hostnames.
```shell
dig axfr internal.inlanefreight.htb @10.129.78.129
```
![](../../Attachements/Pasted%20image%2020260704192739.png)


#### Subdomain Brute Forcing
The individual `A` records with the hostnames can also be found out with the help of a brute-force attack. To do this, we need a list of possible hostnames, which we use to send the requests in order. Such lists are provided, for example, by [SecLists](https://github.com/danielmiessler/SecLists/blob/master/Discovery/DNS/subdomains-top1million-5000.txt).

An option would be to execute a `for-loop` in Bash that lists these entries and sends the corresponding query to the desired DNS server.

```shell
for sub in $(cat /opt/useful/seclists/Discovery/DNS/subdomains-top1million-110000.txt);do dig $sub.inlanefreight.htb @10.129.78.129 | grep -v ';\|SOA' | sed -r '/^\s*$/d' | grep $sub | tee -a subdomains.txt;done
```


DNSEnum for subdomain bruteforce::
```shell
dnsenum --dnsserver 10.129.14.128 --enum -p 0 -s 0 -o subdomains.txt -f /usr/share/wordlists/seclists/Discovery/DNS/subdomains-top1million-110000.txt inlanefreight.htb

```

